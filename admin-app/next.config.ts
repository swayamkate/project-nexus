import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Removed static export to allow Next.js API Routes and Server Actions (required for secure Superadmin tasks)
};

export default nextConfig;
